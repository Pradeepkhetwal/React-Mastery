// This file has purely js code so it's extension is .js .
import React from 'react'

//At first  here we have created context.


const UserContext = React.createContext()

export default UserContext;

// After this we have to create provider for this context which is created in userContextProvider.jsx file.